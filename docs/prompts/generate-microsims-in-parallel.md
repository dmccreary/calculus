
Run the /microsim-generator skill in parallel on all the #### Diagram MicroSim specifications in chapters 4, 5 and 6.  For each Diagram you will find the specification in the <details> XML element after the #### Diagram level 4 header.  Write a log for each microsim to logs/{microsim-name}.md and include a precise timestamp when you started and when you finished.


Run the /microsim-generator skill in parallel on all the #### Diagram MicroSim specifications in chapters 7, 8 and 9.  For each Diagram you will find the specification in the <details> XML element after the #### Diagram level 4 header.  Write a log for each microsim to logs/{microsim-name}.md and include a precise timestamp when you started and when you finished.

Run the /microsim-generator skill in parallel on all the #### Diagram MicroSim specifications in chapters 10, 11, 12, 13, 14 and 15.  For each Diagram you will find the specification in the <details> XML element after the #### Diagram level 4 header.  Write a log for each microsim to logs/{microsim-name}.md and include a precise timestamp when you started and when you finished.

Run the /microsim-generator skill in parallel on all the #### Diagram MicroSim specifications in chapters 16, 17, 18 and 19.  For each #### Diagram level 4 header you will find the specification in the <details markdown="1"> XML element AFTER the #### Diagram level 4 header.  Write a log for each microsim to logs/{microsim-name}.md and include a precise timestamp when you started and when you finished.  Make sure that when each MicroSim is generated an iframe is also placed between the #### Diagram and the <details markdown="1">.

Run the /microsim-generator skill in parallel on all the final #### Diagram MicroSim specifications in chapters 20.  For each #### Diagram level 4 header you will find the specification for the MicroSim in the <details markdown="1"> XML element AFTER the #### Diagram level 4 header.  Write a log for each microsim to logs/{microsim-name}.md and include a precise timestamp when you started and when you finished.  Make sure that when each MicroSim is generated an iframe is also placed between the #### Diagram and the <details markdown="1"> element.

Run the /microsim-generator skill in parallel on all the final #### Diagram MicroSim specifications in chapters 21.  For each #### Diagram level 4 header you will find the specification for the MicroSim in the <details markdown="1"> XML element AFTER the #### Diagram level 4 header.  Write a log for each microsim to logs/{microsim-name}.md and include a precise timestamp when you started and when you finished.  Make sure that when each MicroSim is generated an iframe is also placed between the #### Diagram and the <details markdown="1"> element.

Run the /microsim-generator skill in parallel on all the final #### Diagram MicroSim specifications in chapters 22.  For each #### Diagram level 4 header you will find the specification for the MicroSim in the <details markdown="1"> XML element AFTER the #### Diagram level 4 header.  Write a log for each microsim to logs/{microsim-name}.md and include a precise timestamp when you started and when you finished.  Make sure that when each MicroSim is generated an iframe is also placed between the #### Diagram and the <details markdown="1"> element.

Run the /microsim-generator skill in parallel on all the final #### Diagram MicroSim specifications in chapters 20, 21, 22 and 23.  For each #### Diagram level 4 header you will find the specification for the MicroSim in the <details markdown="1"> XML element AFTER the #### Diagram level 4 header.  Write a log for each microsim to logs/{microsim-name}.md and include a precise timestamp when you started and when you finished.  Make sure that when each MicroSim is generated an iframe is also placed between the #### Diagram and the <details markdown="1"> element.